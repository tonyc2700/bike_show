<h1>All Bikes</h1>
<ul id="bikes">
  <% @bikes.each do |bike| %>
    <li>
      <%= link_to bike.title, bike_path(bike), class: 'bike_title' %>
      <%= link_to "Add a new bike", new_bike_path, class: "new_bike" %>
    </li>
  <% end %>
</ul>

